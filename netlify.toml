[build]
  command = "npx puppeteer browsers install chrome && npm run build"
  publish = "build"

[functions]
  directory = "functions"
  node_bundler = "esbuild"

[functions.snap]
  timeout = 26 
  memory = 1024