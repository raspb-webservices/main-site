[build]
  command = "npm run build"
  publish = "build"

[functions]
  directory = "functions"
  node_bundler = "esbuild"

[functions."*"]
  timeout = 30
  memory = 1024

[[plugins]]
  package = "netlify-plugin-chromium"